{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome for Roll Testing",
            "request": "launch",
            "type": "pwa-chrome",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}